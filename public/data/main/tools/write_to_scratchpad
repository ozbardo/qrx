/* Saves raw text you generated directly into your Working Memory (Scratchpad). Input MUST be: pointer_name | text_content */ var parts = INPUT.split('|'); if (parts.length < 2) return "OBSERVATION: Error. Format must be pointer_name | text_content"; var ptr = parts[0].trim(); var txt = parts.slice(1).join('|').trim(); if (typeof window.agentScratchpad === 'undefined') window.agentScratchpad = {}; window.agentScratchpad[ptr] = txt; return "OBSERVATION: Success. " + txt.length + " bytes saved to Working Memory at pointer $SCRATCHPAD['" + ptr + "']."; 