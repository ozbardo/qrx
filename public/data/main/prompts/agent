#agent?e&w&p=TASK: output HTML wireframe for an autonomous agent
- components:
- header
- model details
- model name
- model host
- apikey (type=password)
- split-view container (use flex row)
- left-panel (for agent internal monologue and tools; no placeholders)
- right-panel (for the final chat with user; no placeholders)
- footer
- resizable textarea
- submit button (must be type="button", DO NOT wrap anything in a <form> tag)
- css: use flex so the split-view fits the space between header and footer
- css: left-panel and right-panel should be 50 percent width and scrollable
- no javascript
- dont include html/head/body tags just the wireframe
&w&a=1&p=TASK: output a SCRIPT tag that populates the header details
- read the value of localStorage.getItem('m') and store it in the model name field
- do same for localStorage.getItem('h')
- and localStorage.getItem('k')
&w&a=1&p=TASK: output a SCRIPT tag defining the base agent tools
- the global functions read(link, db) and keys(query, db) are available to you
- create a global object window.BaseTools = {}
- add a tool named "read_file" to window.BaseTools
- description: "Reads a file. Input MUST be namespace%23filename or just filename" (replace %23 with actual hash character)
- run: async function(input) { let d = null; let k = input; if (input.includes('%23')) { let p = input.split('%23'); d = p[0]; k = p[1]; } return await read(k, d || db) || "File not found"; }
- add a tool named "list_files" to window.BaseTools
- description: "Lists files. Input can be a namespace string, or empty for the current database."
- run: async function(input) { let targetDb = input %3F await getDB(input) : db; return JSON.stringify(await keys(undefined, targetDb)); }
&w&a=1&c=src&p=TASK: output a SCRIPT tag that handles the agent reasoning loop
- the attached CONTEXT gives you clues on how to handle API calls
- reimplement the api call to the LLM (like in gen) using the values from the header inputs
- create a global array window.chatHistory = []
- create an async function runAgent(userMessage)
- bind the submit button and textarea (CTRL+ENTER) to call runAgent(textarea.value) and then clear textarea (call event.preventDefault() so the page never reloads)
- INSIDE runAgent(userMessage):
- append the userMessage to the right-panel DOM
- push { role: "User", content: userMessage } to window.chatHistory
- start a loop with a maximum of 7 iterations (to prevent infinite loops)
- INSIDE the loop: dynamically build available tools to guarantee mesh sync
- let activeTools = Object.entries(window.BaseTools).map(x => x[0] + ' - ' + x[1].description)
- let matchedKeys = await keys(IDBKeyRange.bound('tool', 'tool\\uffff'))
- for each k in matchedKeys: let c = await read(k); let desc = ""; if (c.includes("/*")) { desc = c.split("*/")[0].split("/*")[1].trim(); } activeTools.push(k + ' - ' + desc);
- INSIDE the loop: create a systemPrompt string: "You are an autonomous AI. Output ONLY valid JSON. If the user asks about other namespaces or databases, proactively use tools to investigate. To use a tool, output exactly: {\"thought\": \"reasoning\", \"action\": \"tool_name\", \"action_input\": \"input\"}. For the final answer, output exactly: {\"thought\": \"reasoning\", \"answer\": \"final message\"}. DO NOT wrap your response in markdown blocks."
- INSIDE the loop: append "\nAVAILABLE TOOLS:\n" + activeTools.join('\n') to the systemPrompt
- send systemPrompt + "\n\nCHAT HISTORY:\n" + JSON.stringify(window.chatHistory) to the LLM
- let aiResponse = the raw text from the LLM
- append the aiResponse to the left-panel DOM so we can see it thinking
- extract the JSON object: find the first "{" and last "}" and slice the string, then JSON.parse it.
- if parsing fails, push { role: "System", content: "Error: Invalid JSON output." } to window.chatHistory and continue loop.
- if the parsed object has an "action" property:
- INSIDE a try/catch block:
- let toolResult = ""; let code = "";
- if (window.BaseTools[parsed.action]) { toolResult = await window.BaseTools[parsed.action].run(parsed.action_input); } else { code = await read(parsed.action) || ""; if (!code) throw new Error("Tool not found"); toolResult = await new Function('INPUT', 'return (async () => {' + code + '})()')(parsed.action_input); }
- if (toolResult === undefined) toolResult = "Tool executed successfully (no return value).";
- INSIDE catch: call console.error("Tool Execution Failed:", err, code); and then set toolResult = "Tool Execution Error: " + err.message + "\nStack: " + err.stack + "\nCode evaluated:\n" + code;
- AFTER try/catch: append "OBSERVATION: " + toolResult to the left-panel DOM
- AFTER try/catch: push { role: "System", content: "OBSERVATION: " + toolResult } to window.chatHistory
- continue the loop
- if the parsed object has an "answer" property:
- append parsed.answer to the right-panel DOM
- push { role: "Agent", content: parsed.answer } to window.chatHistory
- break the loop
- AFTER the loop finishes: if the loop hit the maximum 7 iterations without breaking early on an answer, append "System Diagnostics: Agent exhausted 7 reasoning steps. Aborted. Check left-panel for looping traces." to the right-panel DOM and update chatHistory.
- do not autoscroll the page
&w